#include <bits/stdc++.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
using namespace std;
int main ()
{
    string s;
    cin>>s;
    int result, result1,i, sum;
    //int result = s.size();
    int result = s.count('v');
    int result1 = s.count('w');
    sum = (result*1)+(result2*2);
    cout<<sum<<endl;

    return 0;
}


/*for (i=0; i<result; i++)
    {
        if (s[i] =='v')
        {
            c++;
            result1 =result1 + (c*1);
        }
        else if (s[i]=='w')
        {
            m++;
            result2 = result2 + (m*2);
        }
    sum = result1+result2;
    }
    cout<<sum<<endl;*/
